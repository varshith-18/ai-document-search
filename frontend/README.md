This folder is a placeholder for the frontend (React + Tailwind).

Recommended quick start:

1. From this folder run: npx create-vite@latest web --template react
2. cd web
3. npm install
4. Install Tailwind per official docs: https://tailwindcss.com/docs/guides/vite

You can also use Next.js or other frameworks. The initial repo keeps frontend minimal.
